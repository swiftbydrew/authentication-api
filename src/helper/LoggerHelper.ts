export class Logger {
    static prettyLog(message: string) {
        console.log(
`===============================================
        ${message}                       
===============================================`)
    }
}